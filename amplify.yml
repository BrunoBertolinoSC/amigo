frontend:
  phases:
    preBuild:
      commands:
        - echo "Nenhuma dependência"
    build:
      commands:
        - echo "Build simples"
  artifacts:
    baseDirectory: .
    files:
      - '**/*.html'
      - '**/*.js'
      - '**/*.css'
    fallback:
      rewrite: index.html
  cache:
    paths: []
