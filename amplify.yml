frontend:
  phases:
    preBuild:
      commands:
        - echo "Nenhuma dependência"
    build:
      commands:
        - echo "Build simples"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
    # 👇 define index.html como a página principal
    # caso você use rotas baseadas em HTML5
    fallback:
      rewrite: index.html
  cache:
    paths: []
