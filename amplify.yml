version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Nenhuma dependência"
    build:
      commands:
        - echo "Build simples"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
